{
  "name": "cogimp-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "ml": "cd ml_api && python app.py",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && set PORT=3002 && npm start",
    "start": "concurrently \"npm run ml\" \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
